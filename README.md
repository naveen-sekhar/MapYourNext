# MapYourNext Client

This is the React frontend for the MapYourNext application, built with Vite, Tailwind CSS, and Redux Toolkit.

## Setup
1. Copy `.env.example` to `.env` and fill in any necessary tokens.
2. Run `npm install`
3. Run `npm run dev` to start the development server.
